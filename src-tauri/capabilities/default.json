{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core and dialog permissions for the main Figure Composer window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
